# odin-recipes

A simple recipes site consisting of a main index.html file with links to different recipes.
Uses simple html elements like headers, paragraphs, lists, images, and links. No css or javascipt is used.
